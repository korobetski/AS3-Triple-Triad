<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug overlay. Merged into the main manifest for debug builds only, which is what keeps the
    cleartext exception out of anything shippable — see `res/xml/network_security_config.xml`.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
